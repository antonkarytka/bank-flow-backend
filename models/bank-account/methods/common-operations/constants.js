const AMOUNT_ACTION = {
  INCREASE: 'increase',
  DECREASE: 'decrease'
};

const ALLOWED_AMOUNT_ACTIONS = Object.values(AMOUNT_ACTION);

module.exports = {
  AMOUNT_ACTION,
  ALLOWED_AMOUNT_ACTIONS
};